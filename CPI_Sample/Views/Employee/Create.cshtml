@model CPI_Sample.Models.Employee

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Employee</legend>

        <div class="editor-label">
            Initials
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.employeeInitials)
            @Html.ValidationMessageFor(model => model.employeeInitials)
        </div>

        <div class="editor-label">
            Phone #
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.employeePhone)
            @Html.ValidationMessageFor(model => model.employeePhone)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
